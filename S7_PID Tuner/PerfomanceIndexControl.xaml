<UserControl x:Class="S7_PID_Tuner.PerfomanceIndexControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
              d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <CheckBox x:Name="indexCheckBox" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" Content="PerfomanceIndex" IsChecked="{Binding IsChecked, Mode = TwoWay}"/>
            <TextBox x:Name="valueTextBox"  Text="{Binding Path=Value, StringFormat='G6'}" IsEnabled="False" />
        </StackPanel>
    </Grid>
</UserControl>
